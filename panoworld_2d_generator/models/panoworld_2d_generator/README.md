# PanoWorld 2D Generator LoRA

Place `pytorch_lora_weights.safetensors` under the repository-level
`model_ckpt/` directory, or set `PANOWORLD_LORA` when running inference.

This package directory is intentionally kept free of checkpoint files so the
code repository remains lightweight.
